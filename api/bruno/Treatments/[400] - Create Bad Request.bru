meta {
  name: [400] - Create Bad Request
  type: http
  seq: 19
}

post {
  url: {{baseUrl}}/treatments
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Incomplete Treatment"
  }
}

tests {
  test("should return 400 Bad Request", function() {
    expect(res.getStatus()).to.equal(400);
  });
}
