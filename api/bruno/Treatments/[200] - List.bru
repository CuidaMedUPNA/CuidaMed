meta {
  name: [200] - List
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/treatments
  body: none
  auth: inherit
}

tests {
  test("should return 200 OK", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("should return array of treatments", function() {
    expect(res.body).to.be.an('array');
  });
}
