meta {
  name: [400] - List Bad Request
  type: http
  seq: 25
}

get {
  url: {{baseUrl}}/treatments/:treatmentId/intakes
  body: none
  auth: inherit
}

params:path {
  treatmentId: invalid
}

tests {
  test("should return 400 Bad Request", function() {
    expect(res.getStatus()).to.equal(400);
  });
}
